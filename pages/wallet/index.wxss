.wallet-container {
  min-height: 100vh;
  background: #f3f3f3;
  box-sizing: border-box;
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
}

/* 顶部导航栏 */
.nav-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 88rpx;
  padding: 0 32rpx;
  background: #fff;
}

.nav-left {
  width: 40rpx;
  height: 40rpx;
  display: flex;
  align-items: center;
}

.nav-back {
  width: 40rpx;
  height: 40rpx;
}

.nav-title {
  font-size: 34rpx;
  font-weight: 700;
  color: #333;
}

.nav-right {
  display: flex;
  align-items: center;
  gap: 20rpx;
}

.nav-icon {
  width: 34rpx;
  height: 34rpx;
}

/* 余额区域 */
.balance-section {
  border-radius: 16rpx;
  overflow: hidden;
  width: 702rpx;
}

.balance-bg {
  background: url('https://public.jisuhudong.com/minapp/wallet_banner2.png') no-repeat center center;
  background-size: 100% 100%;
  border-radius: 16rpx;
  position: relative;
  width: 702rpx;
  height: 240rpx;
  overflow: hidden;
  margin-top: 38rpx;
}

.balance-main {
  text-align: left;
  position: relative;
  z-index: 1;
  margin: 48rpx 40rpx;
}

.balance-label {
  font-size: 28rpx;
  color: rgba(255, 255, 255);
}

.balance-amount-row {
  display: flex;
  align-items: baseline;
  justify-content: flex-start;
  margin-top: 20rpx;
}

.balance-yuan {
  font-size: 48rpx;
  font-weight: 700;
  color: #fff;
}

.balance-value {
  font-size: 64rpx;
  font-weight: 700;
  color: #fff;
  font-family: 'home-din-pro-700-bold';
  line-height: 1;
}

/* .balance-deco {
  position: absolute;
  right: 26rpx;
  top: 20rpx;
  width: 120rpx;
  height: 90rpx;
  border-radius: 24rpx;
  background: linear-gradient(145deg, rgba(255,255,255,0.35), rgba(255,255,255,0.08));
  transform: rotate(-14deg);
} */

.balance-panel {
  margin-top: 32rpx;
  background: #fff;
  border-radius: 16rpx;
  padding: 32rpx;
}

.balance-sub {
  display: flex;
  gap: 16rpx;
  justify-content: space-between;
}

.balance-sub-item {
  min-height: 96rpx;
  padding: 8rpx 0;
  gap: 10rpx;
  background: transparent;
  border-radius: 0;
  display: flex;
  flex-direction: column;
  flex: 1;
  align-items: center;
}

.sub-label {
  font-size: 22rpx;
  color: #9aa1b1;
  text-align: left;
  line-height: 1.2;
}

.sub-value {
  font-size: 40rpx;
  font-weight: 700;
  color: #333;
  font-family: 'home-din-pro-700-bold';
}

.balance-actions {
  display: flex;
  gap: 24rpx;
  padding: 20rpx 0 0;
}

.action-btn {
  flex: 1;
  height: 80rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin: 0;
  padding: 0;
  border-radius: 40rpx;
  font-size: 30rpx;
  font-weight: 600;
  border: none;
}

.action-btn::after {
  border: none;
}

.action-btn-primary {
  color: #fff;
  background: #ff9f2f;
  box-shadow: none;
}

.action-btn-outline {
  color: #ef8802;
  background: #f4ede2;
  border: none;
  box-shadow: none;
}

/* 交易记录标签 */
.tabs-section {
  background: #fff;
  padding: 0 32rpx;
  margin-top: 24rpx;
  border-radius: 16rpx 16rpx 0 0;
  width: 100%;
}

.tab-group {
  display: flex;
  gap: 48rpx;
  padding: 40rpx 0 0 0;
}

.tab-item {
  position: relative;
  font-size: 28rpx;
  color: #999;
  font-weight: 500;
  padding-bottom: 24rpx;
}

.tab-item.active {
  color: #ef8802;
  font-weight: 700;
}

.tab-item.active::after {
  content: '';
  position: absolute;
  left: 50%;
  bottom: 0;
  transform: translateX(-50%);
  width: 64rpx;
  height: 4rpx;
  background: #ef8802;
  border-radius: 2rpx;
}

/* 交易记录列表 */
.transactions-section {
  padding: 40rpx 24rpx;
  background: #FAFAFA;
  border-radius: 0 0 16rpx 16rpx;
  width: 100%;
}

.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 120rpx 0;
}

.empty-text {
  font-size: 28rpx;
  color: #999;
}

.transaction-list {
  background: transparent;
  border-radius: 0;
  overflow: visible;
}

.transaction-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 24rpx;
  border-bottom: none;
  background: #fff;
  border-radius: 12rpx;
  margin-bottom: 16rpx;
}

.trans-info {
  display: flex;
  flex-direction: column;
  gap: 8rpx;
}

.trans-type {
  font-size: 28rpx;
  color: #333;
  font-weight: 500;
}

.trans-time {
  font-size: 24rpx;
  color: #8C8C98;
}

.trans-amount-group {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 8rpx;
}

.trans-amount-row {
  display: flex;
  align-items: baseline;
  gap: 4rpx;
}

.trans-sign {
  font-size: 28rpx;
  font-weight: 600;
}

.trans-value {
  font-size: 32rpx;
  font-weight: 700;
}

.trans-unit {
  font-size: 22rpx;
}

.trans-sign.income, .trans-value.income, .trans-unit.income {
  color: #35B86B;
}

.trans-sign.expense, .trans-value.expense, .trans-unit.expense {
  color: #EF4444;
}

.trans-fee {
  font-size: 24rpx;
  color: #8C8C98;
}

/* 弹窗样式 */
.modal-mask {
  position: fixed;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  background: rgba(0, 0, 0, 0.5);
  z-index: 20;
  display: flex;
  align-items: center;
  justify-content: center;
}

.modal-container {
  position: fixed;
  width: 100vw;
  height: 100vh;
  z-index: 21;
  display: flex;
  align-items: center;
  justify-content: center;
  left: 0;
  top: 0;
}

.modal-content {
  border-radius: 24rpx;
  background: #fff;
  overflow: hidden;
  width: 640rpx;
}

.modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 32rpx 32rpx 0 32rpx;
}

.modal-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #333;
}

.modal-close {
  width: 48rpx;
  height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 40rpx;
  color: #999;
  line-height: 1;
}

.modal-body {
  padding: 48rpx;
}

.modal-input {
  width: 100%;
  height: 94rpx;
  padding: 0 32rpx;
  border-radius: 16rpx;
  background: #F5F5F7;
  font-size: 28rpx;
  box-sizing: border-box;
}

.modal-tip {
  display: block;
  margin-top: 48rpx;
  font-size: 24rpx;
  color: #8C8C98;
  line-height: 1.6;
}

.modal-submit {
  margin: 0 48rpx 80rpx 48rpx;
  height: 88rpx;
  border-radius: 999rpx;
  background: #ff9f2f;
  color: #fff;
  font-size: 26rpx;
  font-weight: 700;
  display: flex;
  align-items: center;
  justify-content: center;
}

/* 成功弹窗样式 */
.success-content {
  padding-top: 48rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 640rpx;
}

.success-icon {
  width: 117rpx;
  height: 117rpx;
  margin: 58rpx;
}

.success-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #333;
}

.success-desc {
  font-size: 28rpx;
  color: #333;
  text-align: center;
  line-height: 1.6;
  margin-bottom: 48rpx;
  padding: 0 48rpx;
}

.success-amount {
  color: #ff9f2f;
  font-weight: 700;
}

.success-btn {
  width: 100%;
  height: 88rpx;
  border-radius: 0 0 24rpx 24rpx;
  color: #576B95;
  font-size: 30rpx;
  font-weight: 500;
  display: flex;
  align-items: center;
  justify-content: center;
  border-top: 2rpx solid #E5E5E5;
}
