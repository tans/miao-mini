.profile-container {
  min-height: 100vh;
  box-sizing: border-box;
  background: #f3f4f6;
  padding: 0;
  overflow: hidden;
}

.avatar-card,
.info-card {
  background: #fff;
  box-shadow: 0rpx 20rpx 52rpx 0rpx rgba(20, 42, 31, 0.08);
  border-radius: 16rpx 16rpx 16rpx 16rpx;
  margin: 32rpx 24rpx;
  padding: 28rpx;
}

.avatar-card {
  min-height: 260rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.avatar-section {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.avatar {
  width: 160rpx;
  height: 160rpx;
  border-radius: 50%;
  background: #f1f2f5;
}

.avatar-edit {
  margin-top: 24rpx;
  min-width: 152rpx;
  height: 56rpx;
  padding: 0 20rpx;
  border-radius: 28rpx;
  border: 1rpx solid #e7e7e7;
  background: #fff;
  display: flex;
  align-items: center;
  justify-content: center;
}

.avatar-edit-text {
  font-size: 24rpx;
  color: #333;
  font-weight: 600;
  line-height: 1;
}

.info-row {
  display: flex;
  align-items: center;
  min-height: 92rpx;
  border-radius: 14rpx;
  border: 2rpx solid #ececef;
  background: transparent;
  padding: 0 24rpx;
  box-sizing: border-box;
}

.info-row-muted {
  background: #fafafa;
}

.info-row + .info-row {
  margin-top: 24rpx;
}

.info-label {
  width: 140rpx;
  font-size: 28rpx;
  color: #333;
  font-weight: 500;
}

.info-value,
.info-input {
  flex: 1;
  font-size: 30rpx;
  color: #333;
  min-width: 0;
}

.info-copy-btn {
  flex-shrink: 0;
  margin-left: 16rpx;
  padding: 16rpx 24rpx;
  border-radius: 8rpx;
  font-size: 24rpx;
  font-weight: 600;
  color: #ff9f2f;
  background: #FEF4E6;
}

.info-copy-btn-muted {
  color: #9ca3af;
  background: #eef0f3;
}

.info-input {
  height: 92rpx;
}

.info-value-muted {
  color: #9ca3af;
}

.phone-field {
  flex: 1;
  display: flex;
  align-items: center;
  gap: 16rpx;
  min-width: 0;
}

.phone-value {
  display: flex;
  align-items: center;
  min-height: 92rpx;
}

.phone-bind-btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  width: auto;
  min-width: 0;
  height: auto;
  margin-left: 16rpx;
  line-height: 1;
  box-sizing: border-box;
}

.phone-bind-btn::after {
  border: none;
}

.info-placeholder {
  color: #a3a3a3;
  font-weight: 400;
}

.save-section {
  margin-top: 28rpx;
}

.save-btn {
  height: 86rpx;
  border-radius: 96rpx;
  background: #FF9F2F;
  color: #fff;
  font-size: 30rpx;
  line-height: 88rpx;
  text-align: center;
  margin: 32rpx 24rpx;
}

.save-btn::after {
  border: none;
}

/* 兼容可能残留的旧类名引用 */
.user-card {
  background: #fff;
  border-radius: 16rpx;
  padding: 0;
}
