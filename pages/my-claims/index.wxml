<view class="claims-container">
  <!-- Tab 标签 -->
  <view class="tabs">
    <text class="tab {{activeTab === 'all' ? 'active' : ''}}" data-tab="all" bindtap="switchTab">全部</text>
    <text class="tab {{activeTab === 'pending' ? 'active' : ''}}" data-tab="pending" bindtap="switchTab">待提交</text>
    <text class="tab {{activeTab === 'submitted' ? 'active' : ''}}" data-tab="submitted" bindtap="switchTab">待验收</text>
    <text class="tab {{activeTab === 'completed' ? 'active' : ''}}" data-tab="completed" bindtap="switchTab">已完成</text>
  </view>

  <!-- 认领列表 -->
  <mp-cells wx:if="{{claims.length > 0}}">
    <mp-cell wx:for="{{filteredClaims}}" wx:key="id">
      <view slot="title" bindtap="goTaskDetail" data-id="{{item.task_id}}">
        <text class="claim-task-title">{{item.task_title || '任务' + item.task_id}}</text>
        <view class="claim-body">
          <text class="claim-price">¥{{item.unit_price || 0}}</text>
          <text class="claim-time">{{item.created_at}}</text>
        </view>
      </view>
      <view slot="footer" class="claim-actions claim-btns">
        <!-- 待提交状态：显示提交和放弃按钮 -->
        <view wx:if="{{item.status === 1}}" class="claim-item-wrapper">
          <text class="watermark-red watermark-bg">待提交</text>
          <button class="weui-btn weui-btn_mini weui-btn_primary" data-claim-id="{{item.id}}" bindtap="showSubmitModal">提交</button>
          <button class="weui-btn weui-btn_mini weui-btn_default" data-claim-id="{{item.id}}" bindtap="cancelClaim">放弃</button>
        </view>
        <!-- 待验收状态：显示状态和修改链接 -->
        <view wx:if="{{item.status === 2}}" class="claim-item-wrapper">
          <text class="watermark-blue watermark-bg">待验收</text>
          <view class="claim-status-with-action">
            <text class="claim-status status-{{item.status}}">{{getClaimStatusText(item.status)}}</text>
            <text class="modify-link" bindtap="showSubmitModal" data-claim-id="{{item.id}}">修改</text>
          </view>
        </view>
        <!-- 已完成状态 -->
        <view wx:if="{{item.status === 3}}" class="claim-item-wrapper">
          <text class="watermark-gray watermark-bg">已完成</text>
          <text class="claim-status status-{{item.status}}">{{getClaimStatusText(item.status)}}</text>
        </view>
      </view>
    </mp-cell>
  </mp-cells>

  <!-- 空状态 -->
  <mp-msg type="info" title="暂无认领记录" wx:if="{{!loading && claims.length === 0}}" />

  <!-- 提交弹窗 - 使用 WeUI Dialog -->
  <mp-dialog title="提交作品" show="{{showSubmitModal}}" bindbuttontap="onDialogTap" buttons="{{dialogButtons}}">
    <view class="dialog-content">
      <mp-cells>
        <mp-cell>
          <input class="weui-input" placeholder="请输入视频链接" value="{{submitUrl}}" bindinput="onSubmitUrlInput"/>
        </mp-cell>
        <mp-cell>
          <input class="weui-input" placeholder="备注（选填）" value="{{submitNote}}" bindinput="onSubmitNoteInput"/>
        </mp-cell>
      </mp-cells>
    </view>
  </mp-dialog>
</view>