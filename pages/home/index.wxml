<!-- pages/home/index.wxml -->
<view class="home-container">
  <!-- 顶部渐变区域 -->
  <view class="header-gradient">
    <view class="header-top">
      <view class="header-title-wrap">
        <text class="header-title">创意喵｜AI视频众包撮合平台</text>
        <text class="header-subtitle">双向信用管控 100%资金托管</text>
      </view>
      <view class="header-actions">
        <image src="../../images/home/FigmaDDSSlicePNG5f1862ce8b8fd98e7bbbf34fe80d8f63.png" class="msg-icon" />
        <view class="msg-badge">8</view>
      </view>
    </view>
    <!-- Banner轮播 -->
    <scroll-view class="banner-scroll" scroll-x>
      <view class="banner-list">
        <view class="banner-item">
          <image src="../../images/home/FigmaDDSSlicePNGc75faa1c8dcb90839a3c0761ac45a37b.png" class="banner-img" />
          <view class="banner-tag">学会这招，收益稳稳到账</view>
        </view>
        <view class="banner-item">
          <image src="../../images/home/FigmaDDSSlicePNGc75faa1c8dcb90839a3c0761ac45a37b.png" class="banner-img" />
          <view class="banner-tag">学会这招，收益稳稳到账</view>
        </view>
        <view class="banner-item">
          <image src="../../images/home/FigmaDDSSlicePNGc75faa1c8dcb90839a3c0761ac45a37b.png" class="banner-img" />
          <view class="banner-tag">学会这招，收益稳稳到账</view>
        </view>
      </view>
    </scroll-view>
  </view>

  <!-- 分类标签栏 (在排序栏上方) -->
  <scroll-view class="category-bar" scroll-x wx:if="{{industryTags.length > 0}}">
    <view class="category-inner">
      <text class="category-label">推荐</text>
      <view
        wx:for="{{industryTags}}" wx:key="index"
        class="category-tag {{activeIndustry === item ? 'active' : ''}}"
        bindtap="setIndustry" data-industry="{{item}}"
      >{{item}}</view>
    </view>
  </scroll-view>

  <!-- 排序栏 -->
  <view class="sort-bar">
    <view
      class="sort-item {{sort === 'default' ? 'active' : ''}}"
      bindtap="setSort" data-sort="default"
    >默认</view>
    <view
      class="sort-item {{sort === 'created_at' ? 'active' : ''}}"
      bindtap="setSort" data-sort="created_at"
    >最新</view>
    <view
      class="sort-item {{sort === 'hot' ? 'active' : ''}}"
      bindtap="setSort" data-sort="hot"
    >最热</view>
    <view
      class="sort-item {{sort === 'price_desc' ? 'active' : ''}}"
      bindtap="setSort" data-sort="price_desc"
    >价格最高</view>
  </view>

  <!-- 任务列表 -->
  <view class="task-list" wx:if="{{displayTasks.length > 0}}">
    <view
      wx:for="{{displayTasks}}" wx:key="id"
      class="task-card"
      bindtap="goTaskDetail" data-id="{{item.id}}"
    >
      <view class="task-card-body">
        <!-- 标题和价格 -->
        <view class="task-title-row">
          <text class="task-title">{{item.title}}</text>
        </view>
        <view class="task-price-row">
          <text class="task-price">¥{{item.unit_price}}</text>
          <text class="task-price-unit">元/条</text>
        </view>

        <!-- 标签区域 -->
        <view class="task-tags">
          <view class="badge badge-reward" wx:if="{{item.award_price > 0}}">
            <image src="../../images/home/FigmaDDSSlicePNG58cc34ab5d3eaf106c83cb9a4a0ffb1b.png" class="badge-icon" />
            <text>参与奖励:¥{{item.award_price}}</text>
          </view>
          <view class="badge badge-brand">
            <image src="../../images/home/FigmaDDSSlicePNGcae4433a55ff92f1ae3b0e756f4191b6.png" class="badge-icon" />
            <text>品牌名企</text>
          </view>
          <view class="badge badge-trusted">
            <image src="../../images/home/FigmaDDSSlicePNG3ccd7683dca9220cf92fd2f444f08690.png" class="badge-icon" />
            <text>资金已托管</text>
          </view>
        </view>

        <!-- 行业标签 -->
        <view class="industry-tags">
          <text class="industry-tag" wx:for="{{item.industryArray}}" wx:key="index" wx:for-item="tag">{{tag}}</text>
          <text class="style-tag" wx:if="{{item.styleArray.length > 0}}">{{item.styleArray[0]}}</text>
        </view>

        <!-- 报名进度 -->
        <view class="task-progress">
          <text class="progress-label">报名进度</text>
          <view class="progress-info">
            <text class="progress-text">已报名</text>
            <text class="progress-num">{{item.enrolled_count}}</text>
            <text class="progress-text">/{{item.total_count || 10}}人</text>
          </view>
        </view>

        <!-- 底部操作区 -->
        <view class="task-action">
          <view class="task-deadline">
            <image src="../../images/home/FigmaDDSSlicePNG5e5354f18caa98b588ce4f1dd488f0b2.png" class="clock-icon" />
            <text class="deadline-text" wx:if="{{item.countdown}}">{{item.countdown}}后截止投稿</text>
            <text class="deadline-text" wx:else>截止时间未知</text>
          </view>
          <view class="btn-apply">立即报名</view>
        </view>
      </view>
    </view>
  </view>

  <!-- 空状态 -->
  <view class="empty-state" wx:if="{{displayTasks.length === 0 && !loading}}">
    <mp-msg type="info" title="暂无相关任务" />
  </view>

  <!-- 加载更多 -->
  <view wx:if="{{hasMore && activeIndustry === '' && displayTasks.length > 0}}" class="load-more">
    <button class="weui-btn weui-btn_default" bindtap="loadMore" disabled="{{loading}}">
      {{loading ? '加载中...' : '加载更多'}}
    </button>
  </view>

  <!-- 发布任务悬浮按钮 -->
  <view class="fab-container">
    <button class="fab" bindtap="goCreateTask">
      <image src="../../images/home/FigmaDDSSlicePNG454d10efd52a38cba530e202a537c04c.png" class="fab-icon" />
      <text>发布任务</text>
    </button>
  </view>
</view>